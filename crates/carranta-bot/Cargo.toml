[package]
name = "carranta-bot"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Heuristic policy for Carranta: instant, deterministic, in-process."

[dependencies]
carranta-core = { path = "../carranta-core" }

[lints.rust]
unsafe_code = "forbid"

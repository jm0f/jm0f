[package]
name = "carranta-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Carranta rules engine: pure, deterministic, no I/O."

# Deliberately dependency-free. The core must stay compilable to WASM and
# usable from a hot training loop; every dependency added here is paid for by
# every consumer.
[dependencies]

[lints.rust]
unsafe_code = "forbid"

[package]
name = "carranta-analytics"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Metrics derived from Carranta game records: dice fairness, production, ratings."

[dependencies]
carranta-core = { path = "../carranta-core" }
carranta-record = { path = "../carranta-record" }

[dev-dependencies]
carranta-bot = { path = "../carranta-bot" }

[lints.rust]
unsafe_code = "forbid"

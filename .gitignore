# macOS Finder leaves these in every folder it opens.
.DS_Store

# Build output.
/target

# Training runs.
/runs

# Games played locally. The server writes one file per game beside
# itself; they are somebody's afternoon, not source.
/games
